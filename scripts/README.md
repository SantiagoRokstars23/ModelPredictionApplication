# scripts/

Directorio ya previsto desde el diseno original del repositorio (`CLAUDE.md`,
seccion "scripts/"), inicializado formalmente por `BUILD-001`.

Aqui vivira, en una mision futura, el script de importacion que carga los CSV
de `data/processed/` a PostgreSQL (ver
`docs/35-Arquitectura-Oficial-del-Proyecto-Python.md`, seccion 3, justificacion
de `scripts/`).

Ningun script existe todavia -- este archivo es unicamente un marcador de
proposito, sin logica.
